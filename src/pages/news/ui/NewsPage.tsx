import styles from "./styles.module.css";

//const NewsContext=createContext([]);
export default function NewsPage() {
  //console.log(currentPage);
  return <main className={styles.main}>fff</main>;
}
